# Roadmap

## v0.1：可信最小闭环（当前）

- 四态判定与稳定退出码。
- 命令、文件、HTTP、Playwright 检查。
- 任务/配置/验收文件锁和证据新鲜度。
- Codex Hook、Codex SDK 和 GitHub Action 适配。
- 20 例受控误判基准与可运行示例。

## v0.2：真实仓库试用

在新增功能前完成 5–10 个真实仓库、至少 50 次代理任务。记录：

- ShipProof 抓住的真实“假完成”数量和类型。
- 首次配置耗时、每次验证耗时、误报率和环境阻塞率。
- 用户愿意保留的检查器与完全不用的功能。
- 没有 ShipProof 时，问题是否真的会进入评审或生产。

只有真实数据支持后再选择 v0.2 功能，候选包括增量检查、Jest/Vitest/pytest 结构化结果、远程签名锁和 PR 评论。

## 暂不做

- 通用 AI Agent 平台。
- 自建模型、训练框架或底层推理基础设施。
- 多租户控制台、计费和组织管理。
- 与真实需求无关的检查器数量竞赛。

方向判断标准不是“技术更底层”，而是是否能形成独立资产、被陌生项目采用，并持续获得真实失败样本。
